# Internationalization (i18n) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add multi-language support (English, Norwegian, Russian) with URL-based routing, browser detection, and a language switcher dropdown.

**Architecture:** Uses `next-intl` library with Next.js App Router. All pages move under `src/app/[locale]/`. Translation files live in `messages/{locale}/`. Middleware handles locale detection and URL rewriting. A language dropdown in the header lets users switch manually.

**Tech Stack:** next-intl, Next.js 14 App Router, TypeScript, SCSS

**Spec:** `docs/superpowers/specs/2026-04-11-i18n-design.md`

---

## File Map

### New files
- `src/i18n/routing.ts` — locale list and default locale config
- `src/i18n/request.ts` — message loading per locale
- `src/i18n/navigation.ts` — locale-aware Link, useRouter, usePathname
- `src/middleware.ts` — next-intl middleware for locale detection
- `src/components/LanguageSwitcher.tsx` — dropdown component
- `src/assets/scss/blocks/_language-switcher.scss` — styles
- `messages/en/ui.json` — English UI strings
- `messages/en/cards.json` — English card names
- `messages/en/readings.json` — English readings
- `messages/en/plans.json` — English plan text
- `messages/en/legal.json` — English legal text
- `messages/no/ui.json` — Norwegian UI strings
- `messages/no/cards.json` — Norwegian card names
- `messages/no/readings.json` — Norwegian readings
- `messages/no/plans.json` — Norwegian plan text
- `messages/no/legal.json` — Norwegian legal text
- `messages/ru/ui.json` — Russian UI strings
- `messages/ru/cards.json` — Russian card names
- `messages/ru/readings.json` — Russian readings
- `messages/ru/plans.json` — Russian plan text
- `messages/ru/legal.json` — Russian legal text
- `src/app/[locale]/layout.tsx` — new locale-aware root layout
- `src/app/[locale]/page.tsx` — moved main page
- `src/app/[locale]/subscription/page.tsx` — moved subscription page
- `src/app/[locale]/terms/page.tsx` — moved terms page
- `src/app/[locale]/privacy/page.tsx` — moved privacy page

### Modified files
- `next.config.mjs` — wrap with createNextIntlPlugin
- `src/app/layout.tsx` — becomes a thin wrapper (html/body only, no locale logic)
- `src/components/Header.tsx` — add LanguageSwitcher
- `src/components/MainMenu.tsx` — use translations + locale-aware Link
- `src/components/OfferBlock.tsx` — use translations
- `src/components/Tarot.tsx` — use translations
- `src/components/LoginForm.tsx` — use translations + locale-aware Link
- `src/components/Login.tsx` — use translations
- `src/components/UserProfile.tsx` — use translations + locale-aware Link
- `src/components/SubscriptionPlans.tsx` — use translations
- `src/components/Footer.tsx` — use translations + locale-aware Link
- `src/components/PageShell.tsx` — use translations
- `src/components/Logo.tsx` — use locale-aware Link
- `src/data.ts` — remove `name` field from cards
- `src/lib/plans.ts` — remove `name` and `features` fields
- `src/AppProvider.tsx` — update generateReading to use translations
- `src/assets/scss/style.scss` — import language-switcher styles
- `src/types/Types.ts` — remove `name` from Card type

### Deleted files
- `src/tarotReadings.ts` — replaced by `messages/{locale}/readings.json`

---

## Task 1: Install next-intl and set up config scaffolding

**Files:**
- Modify: `package.json`
- Modify: `next.config.mjs`
- Create: `src/i18n/routing.ts`
- Create: `src/i18n/request.ts`
- Create: `src/i18n/navigation.ts`
- Create: `src/middleware.ts`

- [ ] **Step 1: Install next-intl**

```bash
npm install next-intl@3
```

Pin to v3 which supports Next.js 14.

- [ ] **Step 2: Create `src/i18n/routing.ts`**

```typescript
import { defineRouting } from "next-intl/routing";

export const routing = defineRouting({
  locales: ["en", "no", "ru"],
  defaultLocale: "en",
});
```

- [ ] **Step 3: Create `src/i18n/request.ts`**

```typescript
import { getRequestConfig } from "next-intl/server";
import { hasLocale } from "next-intl";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  const messages = {
    ...(await import(`../../messages/${locale}/ui.json`)).default,
    ...(await import(`../../messages/${locale}/cards.json`)).default,
    ...(await import(`../../messages/${locale}/readings.json`)).default,
    ...(await import(`../../messages/${locale}/plans.json`)).default,
    ...(await import(`../../messages/${locale}/legal.json`)).default,
  };

  return { locale, messages };
});
```

- [ ] **Step 4: Create `src/i18n/navigation.ts`**

```typescript
import { createNavigation } from "next-intl/navigation";
import { routing } from "./routing";

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

- [ ] **Step 5: Create `src/middleware.ts`**

```typescript
import createMiddleware from "next-intl/middleware";
import { routing } from "./i18n/routing";

export default createMiddleware(routing);

export const config = {
  matcher: "/((?!api|trpc|_next|_vercel|.*\\..*).*)",
};
```

- [ ] **Step 6: Update `next.config.mjs`**

```javascript
/** @type {import('next').NextConfig} */
import path from 'path';
import { fileURLToPath } from 'url';
import createNextIntlPlugin from 'next-intl/plugin';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const withNextIntl = createNextIntlPlugin();

const nextConfig = {
    webpack(config) {
        config.module.rules.push({
            test: /\.svg$/,
            use: ['@svgr/webpack'],
        });
        return config;
    },
    sassOptions: {
        includePaths: [path.join(__dirname, 'styles')],
    },
    env: {
        OPENAI_API_KEY: process.env.OPENAI_API_KEY,
    },
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 7: Commit**

```bash
git add src/i18n/ src/middleware.ts next.config.mjs package.json package-lock.json
git commit -m "feat: install next-intl and set up i18n scaffolding"
```

---

## Task 2: Create English message files

Extract all hardcoded English strings into JSON message files. These are the source-of-truth that Norwegian and Russian will be translated from.

**Files:**
- Create: `messages/en/ui.json`
- Create: `messages/en/cards.json`
- Create: `messages/en/readings.json`
- Create: `messages/en/plans.json`
- Create: `messages/en/legal.json`

- [ ] **Step 1: Create `messages/en/ui.json`**

```json
{
  "ui": {
    "discoverYourFate": "Discover Your Fate",
    "discover": "Discover",
    "your": "Your",
    "fate": "Fate",
    "shakeDeck": "Shake the Deck",
    "joinTheCircle": "Join the Circle of the Chosen",
    "loading": "Loading...",
    "pricing": "Pricing",
    "revealYourself": "Reveal Yourself",
    "welcome": "Welcome, {name}",
    "mysticOne": "Mystic One",
    "unveilDestiny": "Unveil Your Destiny, Card by Card...",
    "revokeAndRetry": "Revoke and Retry",
    "cardsHaveSpoken": "The Cards Have Spoken",
    "cardsStillSilent": "The cards are still silent...",
    "stepThroughTheVeil": "Step Through the Veil",
    "yourMysticProfile": "Your Mystic Profile",
    "chooseYourPath": "Choose Your Path",
    "oneReadingAtATime": "One reading at a time, or unlimited every day.",
    "mostPopular": "Most popular",
    "currentPlanBtn": "Current plan",
    "comingSoon": "Coming soon",
    "paymentsLaunchingSoon": "Payments launching soon",
    "termsOfService": "Terms of Service",
    "privacyPolicy": "Privacy Policy",
    "upgrade": "Upgrade",
    "name": "Name",
    "email": "Email",
    "memberSince": "Member since",
    "currentPlan": "Current plan",
    "password": "Password",
    "save": "Save",
    "saving": "Saving...",
    "cancel": "Cancel",
    "changePassword": "Change password",
    "setPassword": "Set password",
    "slipIntoShadows": "Slip Into the Shadows",
    "currentPasswordPlaceholder": "Current password",
    "newPasswordPlaceholder": "New password",
    "confirmPasswordPlaceholder": "Confirm new password",
    "nameCannotBeEmpty": "Name cannot be empty",
    "failedToUpdateName": "Failed to update name",
    "passwordMinLength": "Password must be at least 8 characters",
    "passwordsDoNotMatch": "Passwords do not match",
    "failedToUpdatePassword": "Failed to update password",
    "passwordUpdated": "Password updated",
    "whatShallWeCallYou": "What Shall We Call You?",
    "pledgeYourSoul": "Pledge Your Soul (Or Just Your Email)",
    "enchantedPhrase": "Enchanted Phrase",
    "agreeToTerms": "I agree to the {terms} and {privacy}.",
    "acceptTermsError": "Please accept the Terms of Service and Privacy Policy to continue.",
    "invalidCredentials": "Invalid email or password",
    "somethingWentWrong": "Something went wrong. Please try again.",
    "channeling": "Channeling...",
    "beginTheRitual": "Begin the Ritual",
    "completeTheRitual": "Complete the Ritual",
    "letGoogleSpeak": "Let Google Speak Your Name",
    "alreadyInitiated": "Already initiated? Enter the Sanctum",
    "newToTheCraft": "New to the craft? Join the Circle of the Chosen",
    "drawThreeCards": "Draw three cards to receive your reading.",
    "spiritsUnclear": "The spirits are unclear. Please draw again.",
    "language": "Language"
  }
}
```

- [ ] **Step 2: Create `messages/en/cards.json`**

Extract all 78 card names from `src/data.ts`, keyed by card id:

```json
{
  "cards": {
    "arcana-0": "The Fool",
    "arcana-1": "The Magician",
    "arcana-2": "The High Priestess",
    "arcana-3": "The Empress",
    "arcana-4": "The Emperor",
    "arcana-5": "The Hierophant",
    "arcana-6": "The Lovers",
    "arcana-7": "The Chariot",
    "arcana-8": "Justice",
    "arcana-9": "The Hermit",
    "arcana-10": "Wheel of Fortune",
    "arcana-11": "Strength",
    "arcana-12": "The Hanged Man",
    "arcana-13": "Death",
    "arcana-14": "Temperance",
    "arcana-15": "The Devil",
    "arcana-16": "The Tower",
    "arcana-17": "The Star",
    "arcana-18": "The Moon",
    "arcana-19": "The Sun",
    "arcana-20": "Judgement",
    "arcana-21": "The World",
    "chalices-1": "Ace of Chalices",
    "chalices-2": "Two of Chalices",
    "chalices-3": "Three of Chalices",
    "chalices-4": "Four of Chalices",
    "chalices-5": "Five of Chalices",
    "chalices-6": "Six of Chalices",
    "chalices-7": "Seven of Chalices",
    "chalices-8": "Eight of Chalices",
    "chalices-9": "Nine of Chalices",
    "chalices-10": "Ten of Chalices",
    "chalices-page": "Page of Chalices",
    "chalices-knight": "Knight of Chalices",
    "chalices-queen": "Queen of Chalices",
    "chalices-king": "King of Chalices",
    "swords-1": "Ace of Swords",
    "swords-2": "Two of Swords",
    "swords-3": "Three of Swords",
    "swords-4": "Four of Swords",
    "swords-5": "Five of Swords",
    "swords-6": "Six of Swords",
    "swords-7": "Seven of Swords",
    "swords-8": "Eight of Swords",
    "swords-9": "Nine of Swords",
    "swords-10": "Ten of Swords",
    "swords-page": "Page of Swords",
    "swords-knight": "Knight of Swords",
    "swords-queen": "Queen of Swords",
    "swords-king": "King of Swords",
    "pentacles-1": "Ace of Pentacles",
    "pentacles-2": "Two of Pentacles",
    "pentacles-3": "Three of Pentacles",
    "pentacles-4": "Four of Pentacles",
    "pentacles-5": "Five of Pentacles",
    "pentacles-6": "Six of Pentacles",
    "pentacles-7": "Seven of Pentacles",
    "pentacles-8": "Eight of Pentacles",
    "pentacles-9": "Nine of Pentacles",
    "pentacles-10": "Ten of Pentacles",
    "pentacles-page": "Page of Pentacles",
    "pentacles-knight": "Knight of Pentacles",
    "pentacles-queen": "Queen of Pentacles",
    "pentacles-king": "King of Pentacles",
    "wands-1": "Ace of Wands",
    "wands-2": "Two of Wands",
    "wands-3": "Three of Wands",
    "wands-4": "Four of Wands",
    "wands-5": "Five of Wands",
    "wands-6": "Six of Wands",
    "wands-7": "Seven of Wands",
    "wands-8": "Eight of Wands",
    "wands-9": "Nine of Wands",
    "wands-10": "Ten of Wands",
    "wands-page": "Page of Wands",
    "wands-knight": "Knight of Wands",
    "wands-queen": "Queen of Wands",
    "wands-king": "King of Wands"
  }
}
```

- [ ] **Step 3: Create `messages/en/readings.json`**

Extract all 78 card readings from `src/tarotReadings.ts` plus reading templates. Structure:

```json
{
  "readings": {
    "arcana-0": {
      "keywords": "beginnings, innocence, spontaneity, free spirit",
      "meaning": "The Fool speaks of new beginnings and untapped potential. You stand at the edge of something extraordinary, unburdened by fear or doubt. Trust in the unknown — the universe rewards those who dare to leap.",
      "past": "A bold leap of faith set the stage for where you are now. That moment of fearless innocence, when you chose the unknown over the familiar, planted seeds that continue to grow.",
      "present": "You are being called to embrace a fresh start. Something beckons you toward the edge of the known world — release your grip on certainty and let the wind carry you.",
      "future": "A thrilling new chapter awaits you. The path ahead is uncharted, and that is precisely its gift. Approach what comes with the wonder of a child and the courage of an adventurer."
    }
  },
  "readingTemplates": {
    "intros": [
      "The cards have been drawn, and the tapestry of your fate unfolds before us. {card1}, {card2}, and {card3} — a combination of striking power and undeniable significance.",
      "Three cards emerge from the shadows of the deck: {card1}, {card2}, and {card3}. Together they weave a story that the universe has been waiting to tell you.",
      "The veil parts, and three messengers step forward: {card1}, {card2}, and {card3}. Their combined wisdom speaks directly to the forces shaping your destiny.",
      "From the infinite possibilities of the deck, {card1}, {card2}, and {card3} have chosen you. Yes — the cards choose the reader as much as the reader chooses the cards.",
      "A powerful trinity reveals itself: {card1}, {card2}, and {card3}. The energy between these three cards crackles with meaning — listen closely to what they say.",
      "The stars have aligned to bring forth {card1}, {card2}, and {card3}. This is no accident — these cards carry a message that has been seeking you.",
      "Behold the three cards that emerge from the cosmic shuffle: {card1}, {card2}, and {card3}. Their combined energy paints a portrait of your journey with remarkable clarity.",
      "The deck has spoken with conviction, placing {card1}, {card2}, and {card3} before you. Each card alone is powerful — together, they form a revelation."
    ],
    "bridges": [
      "As we move from past to present, the energy shifts. What was planted now bears fruit in the current moment.",
      "The threads of the past weave into the fabric of your present reality. Notice how one flows into the other.",
      "From the echoes of what was, we arrive at the pulse of what is. The connection between these energies is unmistakable.",
      "The past has set the stage, and now the spotlight falls on this very moment. The cards speak of your present with urgency."
    ],
    "futureBridges": [
      "And now the cards peer beyond the veil of time to illuminate what approaches.",
      "The final card lifts its gaze toward the horizon, revealing the shape of things to come.",
      "What the past planted and the present tends, the future will harvest. The third card speaks of what awaits.",
      "The most mysterious card of all — the one that speaks to what has not yet happened — reveals its message."
    ],
    "closings": [
      "Together, these three cards paint a journey of remarkable significance. The forces at work in your life are powerful, purposeful, and aligned with something greater than chance.",
      "The reading is complete, but its echoes will continue to resonate. Carry the wisdom of these three cards as you navigate the path ahead — they have given you everything you need.",
      "The cards have spoken their truth. This trinity of wisdom — spanning past, present, and future — offers you a compass for the journey ahead. Trust what has been revealed.",
      "This powerful spread reveals that the forces surrounding you are moving with intention and purpose. The universe has offered its guidance — the rest is in your hands.",
      "The cards fall silent, but their message lingers in the air. What has been revealed is both a mirror and a map — reflecting where you have been and illuminating where you are going."
    ],
    "pastPrefix": "Your first card, {card}, speaks to the energies of your past. ",
    "presentPrefix": "{card} stands in the position of the present. ",
    "futurePrefix": "{card} reveals what the future holds. "
  }
}
```

Note: `keywords` is stored as a comma-separated string (not an array) because `next-intl` messages are strings. The full `readings.json` must include all 78 cards — copy every entry from `src/tarotReadings.ts`.

- [ ] **Step 4: Create `messages/en/plans.json`**

```json
{
  "plans": {
    "FREE": {
      "name": "Free",
      "features": ["3 readings per day", "Resets at midnight"]
    },
    "SINGLE": {
      "name": "Single reading",
      "features": ["One extra reading", "No subscription"]
    },
    "MONTHLY": {
      "name": "Monthly",
      "features": [
        "Unlimited readings",
        "Reading history",
        "Choose your deck",
        "Choose your diviner",
        "Long-form interpretations",
        "Daily card email",
        "Export readings as PDF",
        "Favorites & personal notes",
        "Reminder notifications",
        "Ad-free"
      ]
    },
    "YEARLY": {
      "name": "Yearly",
      "features": [
        "Everything in Monthly",
        "Save 58% vs monthly",
        "Exclusive seasonal decks",
        "Early access to new diviners & decks"
      ]
    }
  }
}
```

Note: `plans.json` uses arrays for features (not indexed keys) because the plan display iterates over them. The `useTranslations` hook supports `t.raw()` for accessing raw arrays.

- [ ] **Step 5: Create `messages/en/legal.json`**

Extract the full Terms of Service and Privacy Policy content from `src/app/terms/page.tsx` and `src/app/privacy/page.tsx`:

```json
{
  "legal": {
    "terms": {
      "title": "Terms of Service",
      "pageTitle": "Terms of Service | Tarot",
      "pageDescription": "Terms of Service for the Tarot reading app.",
      "lastUpdated": "Last updated: April 6, 2026",
      "intro": "Welcome to Tarot (\"the Service\", \"we\", \"us\"). By accessing or using the Service, you agree to be bound by these Terms of Service (\"Terms\"). If you do not agree, please do not use the Service.",
      "entertainment": {
        "title": "1. Entertainment Only",
        "content": "The Service provides tarot card readings and related content strictly for entertainment purposes. Readings are not a substitute for professional advice, including but not limited to medical, legal, psychological, or financial advice. You are solely responsible for any decisions you make based on content generated by the Service."
      },
      "eligibility": {
        "title": "2. Eligibility",
        "content": "You must be at least 16 years old to use the Service. By using the Service you represent that you meet this requirement."
      },
      "accounts": {
        "title": "3. Accounts",
        "content": "Some features require you to create an account. You agree to provide accurate information, keep your credentials secure, and notify us of any unauthorized use. You are responsible for all activity under your account. We may suspend or terminate accounts that violate these Terms."
      },
      "acceptableUse": {
        "title": "4. Acceptable Use",
        "intro": "You agree not to:",
        "items": [
          "Use the Service for any unlawful purpose;",
          "Attempt to disrupt, reverse-engineer, or gain unauthorized access to the Service;",
          "Upload malicious content or harass other users;",
          "Scrape, copy, or resell content from the Service without permission."
        ]
      },
      "intellectualProperty": {
        "title": "5. Intellectual Property",
        "content": "All content, design, text, graphics, and code on the Service are owned by us or our licensors and are protected by copyright and other laws. You receive a limited, non-exclusive, non-transferable license to use the Service for personal, non-commercial purposes."
      },
      "userContent": {
        "title": "6. User Content",
        "content": "If you submit content (questions, notes, feedback), you grant us a worldwide, royalty-free license to use, store, and display it solely for the purpose of operating and improving the Service."
      },
      "disclaimers": {
        "title": "7. Disclaimers",
        "content": "The Service is provided \"as is\" without warranties of any kind. We do not guarantee accuracy, availability, or fitness for a particular purpose. Tarot readings are generated for entertainment and carry no predictive guarantee."
      },
      "liability": {
        "title": "8. Limitation of Liability",
        "content": "To the fullest extent permitted by law, we shall not be liable for any indirect, incidental, special, or consequential damages arising out of your use of the Service. Our total liability shall not exceed the amount you paid us in the twelve months preceding the claim, or EUR 50 if you paid nothing."
      },
      "changes": {
        "title": "9. Changes to the Service or Terms",
        "content": "We may modify the Service or these Terms at any time. Material changes will be announced on this page with an updated \"last updated\" date. Continued use after changes constitutes acceptance."
      },
      "termination": {
        "title": "10. Termination",
        "content": "You may stop using the Service at any time. We may suspend or terminate your access if you violate these Terms."
      },
      "governingLaw": {
        "title": "11. Governing Law",
        "content": "These Terms are governed by the laws of the European Union and the country where the Service operator is established, without regard to conflict-of-law principles. EU consumers retain the protections of their local mandatory law."
      },
      "contact": {
        "title": "12. Contact",
        "content": "For questions about these Terms, contact us at {email}."
      },
      "note": "Note: This is a draft template and not legal advice. Please have it reviewed by a qualified lawyer before launch."
    },
    "privacy": {
      "title": "Privacy Policy",
      "pageTitle": "Privacy Policy | Tarot",
      "pageDescription": "Privacy Policy for the Tarot reading app.",
      "lastUpdated": "Last updated: April 6, 2026",
      "intro": "This Privacy Policy explains how Tarot (\"we\", \"us\") collects, uses, and protects your personal data when you use our Service. We comply with the EU General Data Protection Regulation (GDPR) and, where applicable, the California Consumer Privacy Act (CCPA).",
      "dataController": {
        "title": "1. Data Controller",
        "content": "The data controller responsible for your personal data is the operator of this Service. For privacy inquiries, contact us at {email}."
      },
      "dataWeCollect": {
        "title": "2. Data We Collect",
        "items": [
          "Account data: email address, display name, hashed password (or third-party OAuth identifier).",
          "Usage data: reading history, questions you submit, timestamps, and preferences.",
          "Technical data: IP address, browser type, device information, and basic analytics.",
          "Cookies: authentication session cookies and, where applicable, analytics cookies (only with your consent)."
        ]
      },
      "legalBasis": {
        "title": "3. Legal Basis for Processing (GDPR)",
        "items": [
          "Contract: to provide the Service you requested (Art. 6(1)(b) GDPR).",
          "Consent: for optional analytics and marketing (Art. 6(1)(a) GDPR).",
          "Legitimate interest: to secure the Service and prevent abuse (Art. 6(1)(f) GDPR).",
          "Legal obligation: where required by law (Art. 6(1)(c) GDPR)."
        ]
      },
      "howWeUse": {
        "title": "4. How We Use Your Data",
        "items": [
          "To create and manage your account;",
          "To provide tarot readings and related features;",
          "To communicate important updates about the Service;",
          "To secure the Service and prevent fraud or abuse;",
          "To comply with legal obligations."
        ]
      },
      "thirdParty": {
        "title": "5. Third-Party Processors",
        "intro": "We use trusted service providers to operate the Service. These processors act on our behalf under data processing agreements:",
        "items": [
          "Vercel Inc. — hosting and infrastructure (USA / EU regions);",
          "PostgreSQL database provider — data storage (via Vercel Marketplace);",
          "NextAuth OAuth providers — for third-party sign-in, if used (e.g., Google);",
          "Email delivery provider — for transactional emails, if applicable."
        ]
      },
      "transfers": {
        "title": "6. International Transfers",
        "content": "Some processors may be located outside the European Economic Area. Where this occurs, we rely on Standard Contractual Clauses or equivalent safeguards as required by GDPR."
      },
      "retention": {
        "title": "7. Data Retention",
        "content": "We retain account data for as long as your account is active. If you delete your account, we delete or anonymize personal data within 30 days, except where retention is required by law."
      },
      "yourRights": {
        "title": "8. Your Rights (GDPR / CCPA)",
        "intro": "You have the right to:",
        "items": [
          "Access the personal data we hold about you;",
          "Request correction of inaccurate data;",
          "Request deletion (\"right to be forgotten\");",
          "Restrict or object to processing;",
          "Data portability;",
          "Withdraw consent at any time;",
          "Lodge a complaint with your local supervisory authority."
        ],
        "ccpa": "California residents have additional rights under the CCPA, including the right to know what personal data we collect and the right to opt out of any sale of personal data. We do not sell personal data."
      },
      "cookies": {
        "title": "9. Cookies",
        "content": "We use strictly necessary cookies for authentication. Optional analytics cookies are only set after you give consent via our cookie banner. You can withdraw consent at any time through your browser settings."
      },
      "security": {
        "title": "10. Security",
        "content": "We implement reasonable technical and organizational measures to protect your data, including encryption in transit (HTTPS), hashed passwords, and access controls. No system is perfectly secure, and we cannot guarantee absolute security."
      },
      "children": {
        "title": "11. Children",
        "content": "The Service is not directed to children under 16. We do not knowingly collect personal data from children. If you believe a child has provided us with data, please contact us for removal."
      },
      "policyChanges": {
        "title": "12. Changes to This Policy",
        "content": "We may update this Privacy Policy. Material changes will be announced on this page with an updated \"last updated\" date."
      },
      "privacyContact": {
        "title": "13. Contact",
        "content": "For any privacy-related questions or to exercise your rights, contact {email}."
      },
      "note": "Note: This is a draft template and not legal advice. Please have it reviewed by a qualified lawyer before launch, and fill in the actual company name, address, and contact details."
    }
  }
}
```

- [ ] **Step 6: Commit**

```bash
git add messages/en/
git commit -m "feat: create English message files for i18n"
```

---

## Task 3: Create Norwegian translation files

**Files:**
- Create: `messages/no/ui.json`
- Create: `messages/no/cards.json`
- Create: `messages/no/readings.json`
- Create: `messages/no/plans.json`
- Create: `messages/no/legal.json`

- [ ] **Step 1: Create all Norwegian translation files**

Translate every string from the English files into Norwegian (Bokmal). The JSON structure must be identical to the English files — same keys, same nesting. Only values change.

For `ui.json`: translate all UI strings. Keep the `{name}`, `{terms}`, `{privacy}`, `{card}`, `{card1}`, `{card2}`, `{card3}` interpolation placeholders exactly as-is.

For `cards.json`: translate all 78 card names. Major Arcana have traditional Norwegian names (e.g., "Narren", "Magikeren"). Minor Arcana: translate suit names (Chalices→Beger, Swords→Sverd, Pentacles→Pentakler, Wands→Staver) and ranks (Ace→Ess, Page→Knekt, Knight→Ridder, Queen→Dronning, King→Konge).

For `readings.json`: translate all 78 card readings (keywords, meaning, past, present, future) and all reading templates (intros, bridges, futureBridges, closings, prefixes). Maintain the mystical/poetic tone.

For `plans.json`: translate plan names and feature lists.

For `legal.json`: translate all terms and privacy content. Keep legal accuracy.

- [ ] **Step 2: Commit**

```bash
git add messages/no/
git commit -m "feat: add Norwegian translations"
```

---

## Task 4: Create Russian translation files

**Files:**
- Create: `messages/ru/ui.json`
- Create: `messages/ru/cards.json`
- Create: `messages/ru/readings.json`
- Create: `messages/ru/plans.json`
- Create: `messages/ru/legal.json`

- [ ] **Step 1: Create all Russian translation files**

Same approach as Task 3 but in Russian. The JSON structure must be identical to the English files.

For `cards.json`: Major Arcana have traditional Russian names (e.g., "Шут", "Маг"). Minor Arcana suits: Chalices→Кубки, Swords→Мечи, Pentacles→Пентакли, Wands→Жезлы. Ranks: Ace→Туз, Page→Паж, Knight→Рыцарь, Queen→Королева, King→Король.

For `readings.json`: translate maintaining mystical/poetic tone in Russian.

- [ ] **Step 2: Commit**

```bash
git add messages/ru/
git commit -m "feat: add Russian translations"
```

---

## Task 5: Move pages under `[locale]/` and set up layout

**Files:**
- Modify: `src/app/layout.tsx` — strip down to html/body shell
- Create: `src/app/[locale]/layout.tsx` — locale-aware layout with NextIntlClientProvider
- Move: `src/app/page.tsx` → `src/app/[locale]/page.tsx`
- Move: `src/app/subscription/page.tsx` → `src/app/[locale]/subscription/page.tsx`
- Move: `src/app/terms/page.tsx` → `src/app/[locale]/terms/page.tsx`
- Move: `src/app/privacy/page.tsx` → `src/app/[locale]/privacy/page.tsx`

- [ ] **Step 1: Update `src/app/layout.tsx` to a thin shell**

```typescript
import type { Metadata } from "next";
import "../assets/scss/style.scss";

export const metadata: Metadata = {
  title: "Tarot",
  description: "Unveil the mysteries of your future with our mystical tarot reading app.",
  icons: {
    icon: "/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
```

- [ ] **Step 2: Create `src/app/[locale]/layout.tsx`**

```typescript
import { Raleway } from "next/font/google";
import { NextIntlClientProvider, hasLocale } from "next-intl";
import { setRequestLocale } from "next-intl/server";
import { notFound } from "next/navigation";
import { routing } from "@/i18n/routing";

const raleway = Raleway({ subsets: ["latin", "cyrillic"] });

type Props = {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
};

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({ children, params }: Props) {
  const { locale } = await params;
  if (!hasLocale(routing.locales, locale)) {
    notFound();
  }

  setRequestLocale(locale);

  return (
    <html lang={locale}>
      <body className={raleway.className}>
        <NextIntlClientProvider>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

Note: Add `"cyrillic"` subset to Raleway for Russian text support.

- [ ] **Step 3: Move page files**

Move the four page files into the `[locale]` directory. The content of each file stays the same for now — translations will be wired up in later tasks.

```bash
mkdir -p src/app/\[locale\]/subscription src/app/\[locale\]/terms src/app/\[locale\]/privacy
git mv src/app/page.tsx src/app/\[locale\]/page.tsx
git mv src/app/subscription/page.tsx src/app/\[locale\]/subscription/page.tsx
git mv src/app/terms/page.tsx src/app/\[locale\]/terms/page.tsx
git mv src/app/privacy/page.tsx src/app/\[locale\]/privacy/page.tsx
```

- [ ] **Step 4: Verify build**

```bash
npm run build
```

Expected: Build succeeds. Pages now render under `/en/`, `/no/`, `/ru/` prefixes.

- [ ] **Step 5: Commit**

```bash
git add src/app/
git commit -m "feat: move pages under [locale] with next-intl layout"
```

---

## Task 6: Update data files to remove translatable fields

**Files:**
- Modify: `src/data.ts` — remove `name` from cards
- Modify: `src/types/Types.ts` — remove `name` from Card type
- Modify: `src/lib/plans.ts` — remove `name` and `features`

- [ ] **Step 1: Check `src/types/Types.ts` for the Card type**

Read the file to find the exact Card type definition.

- [ ] **Step 2: Remove `name` from Card type in `src/types/Types.ts`**

Remove the `name` field from the `Card` type. Keep `id`, `image`, `value`, and any other fields.

- [ ] **Step 3: Remove `name` from all card entries in `src/data.ts`**

Remove the `name` property from all 78 card objects in the `tarots` array. Each card should only have `id`, `image`, `value`, and optionally `suite`.

- [ ] **Step 4: Update `src/lib/plans.ts`**

Remove `name` and `features` from the `Plan` type and all `PLANS` entries. Keep `id`, `priceLabel`, `interval`, and `highlight`.

```typescript
export type PlanId = "FREE" | "SINGLE" | "MONTHLY" | "YEARLY";

export type Plan = {
  id: PlanId;
  priceLabel: string;
  interval: "one-time" | "month" | "year" | null;
  highlight?: boolean;
};

export const PLANS: Record<PlanId, Plan> = {
  FREE: {
    id: "FREE",
    priceLabel: "$0",
    interval: null,
  },
  SINGLE: {
    id: "SINGLE",
    priceLabel: "$1",
    interval: "one-time",
  },
  MONTHLY: {
    id: "MONTHLY",
    priceLabel: "$10",
    interval: "month",
  },
  YEARLY: {
    id: "YEARLY",
    priceLabel: "$50",
    interval: "year",
    highlight: true,
  },
};

export const PLAN_ORDER: PlanId[] = ["FREE", "SINGLE", "MONTHLY", "YEARLY"];
```

- [ ] **Step 5: Fix all TypeScript errors caused by removing `name`**

Search for references to `card.name`, `plan.name`, `plan.features` throughout the codebase. These will be fixed in Tasks 7-10 when wiring up translations, but note any compile errors here so they are expected.

- [ ] **Step 6: Commit**

```bash
git add src/data.ts src/types/Types.ts src/lib/plans.ts
git commit -m "refactor: remove translatable fields from data files"
```

---

## Task 7: Delete `tarotReadings.ts` and update `generateReading`

**Files:**
- Delete: `src/tarotReadings.ts`
- Modify: `src/AppProvider.tsx` — update reading generation to use translations

- [ ] **Step 1: Delete `src/tarotReadings.ts`**

```bash
git rm src/tarotReadings.ts
```

- [ ] **Step 2: Update `src/AppProvider.tsx`**

The `generateReading` function currently lives in `tarotReadings.ts` and is called from `AppProvider`. Since it now needs translations, move the reading generation logic into a new utility that accepts translated messages.

Create a new file `src/lib/generateReading.ts`:

```typescript
type ReadingMessages = {
  readings: Record<string, {
    past: string;
    present: string;
    future: string;
  }>;
  readingTemplates: {
    intros: string[];
    bridges: string[];
    futureBridges: string[];
    closings: string[];
    pastPrefix: string;
    presentPrefix: string;
    futurePrefix: string;
  };
  cards: Record<string, string>;
};

export function generateReading(
  cardIds: { id: string }[],
  messages: ReadingMessages,
  fallbackError: string,
  fallbackUnclear: string,
): string {
  if (cardIds.length !== 3) return fallbackError;

  const [c1, c2, c3] = cardIds;
  const r1 = messages.readings[c1.id];
  const r2 = messages.readings[c2.id];
  const r3 = messages.readings[c3.id];

  if (!r1 || !r2 || !r3) return fallbackUnclear;

  const card1Name = messages.cards[c1.id];
  const card2Name = messages.cards[c2.id];
  const card3Name = messages.cards[c3.id];

  const pick = (arr: string[]) => arr[Math.floor(Math.random() * arr.length)];

  const intro = pick(messages.readingTemplates.intros)
    .replace("{card1}", card1Name)
    .replace("{card2}", card2Name)
    .replace("{card3}", card3Name);

  const bridge = pick(messages.readingTemplates.bridges);
  const futureBridge = pick(messages.readingTemplates.futureBridges);
  const closing = pick(messages.readingTemplates.closings);

  const pastPrefix = messages.readingTemplates.pastPrefix.replace("{card}", card1Name);
  const presentPrefix = messages.readingTemplates.presentPrefix.replace("{card}", card2Name);
  const futurePrefix = messages.readingTemplates.futurePrefix.replace("{card}", card3Name);

  return [
    intro,
    "",
    `${pastPrefix}${r1.past}`,
    "",
    bridge,
    "",
    `${presentPrefix}${r2.present}`,
    "",
    futureBridge,
    "",
    `${futurePrefix}${r3.future}`,
    "",
    closing,
  ].join("\n");
}
```

- [ ] **Step 3: Update `src/AppProvider.tsx` to use the new function**

The AppProvider needs access to translations. Since it's a client component, it can use `useMessages()` from `next-intl` to get the loaded messages and pass them to `generateReading`:

```typescript
import React, {createContext, ReactNode, useContext, useEffect, useState} from 'react';
import {Card} from "@/types/Types";
import {tarots} from "@/data";
import {useMessages} from "next-intl";
import {generateReading} from "@/lib/generateReading";

// ... AppState and context types stay the same ...

export function AppProvider({ children }: AppProviderProps) {
    const [state, setState] = useState<AppState>(defaultState.state);
    const messages = useMessages();

    useEffect(() => {
        if (state.chosenCards.length > 0) {
            setState(prevState => ({
                ...prevState,
                resetFlipped: true,
                isPredictionReady: false,
                isResponseLoading: true,
            }));

            const response = generateReading(
                state.chosenCards,
                messages as any,
                (messages as any).ui?.drawThreeCards ?? "Draw three cards to receive your reading.",
                (messages as any).ui?.spiritsUnclear ?? "The spirits are unclear. Please draw again.",
            );
            setState(prevState => ({
                ...prevState,
                isResponseLoading: false,
                response: response,
            }));
        }
    }, [state.chosenCards, messages]);

    // ... rest stays the same
}
```

- [ ] **Step 4: Commit**

```bash
git add src/lib/generateReading.ts src/AppProvider.tsx
git rm src/tarotReadings.ts
git commit -m "refactor: replace tarotReadings.ts with translation-based generateReading"
```

---

## Task 8: Wire up translations in all components

This is the largest task — every component with hardcoded English text gets `useTranslations`. Also switch all `Link` imports to locale-aware versions.

**Files:**
- Modify: `src/components/OfferBlock.tsx`
- Modify: `src/components/Tarot.tsx`
- Modify: `src/components/MainMenu.tsx`
- Modify: `src/components/LoginForm.tsx`
- Modify: `src/components/Login.tsx`
- Modify: `src/components/UserProfile.tsx`
- Modify: `src/components/SubscriptionPlans.tsx`
- Modify: `src/components/Footer.tsx`
- Modify: `src/components/PageShell.tsx`
- Modify: `src/components/Logo.tsx`
- Modify: `src/app/[locale]/page.tsx`

- [ ] **Step 1: Update `src/components/Logo.tsx`**

Switch `Link` to locale-aware version:

```typescript
import { Link } from "@/i18n/navigation";
import LogoImg from "../assets/svg/logo.svg";
export default function Logo() {
    return (
        <Link href="/" className="logo">
            <LogoImg />
        </Link>
    );
};
```

- [ ] **Step 2: Update `src/components/Footer.tsx`**

```typescript
"use client";
import { Link } from "@/i18n/navigation";
import { useTranslations } from "next-intl";

export default function Footer() {
    const t = useTranslations("ui");
    return (
        <footer className="main-footer container">
            <p className="main-footer__legal">
                <Link className="main-footer__link" href="/terms">{t("termsOfService")}</Link>
                {" · "}
                <Link className="main-footer__link" href="/privacy">{t("privacyPolicy")}</Link>
            </p>
        </footer>
    );
};
```

- [ ] **Step 3: Update `src/components/MainMenu.tsx`**

```typescript
"use client";

import { Link } from "@/i18n/navigation";
import { useSession } from "next-auth/react";
import { useTranslations } from "next-intl";

type MainMenuProps = {
    onOpenLogin: () => void;
    onOpenProfile: () => void;
};

export default function MainMenu({ onOpenLogin, onOpenProfile }: MainMenuProps) {
    const { data: session, status } = useSession();
    const t = useTranslations("ui");

    return (
        <nav className="main-menu">
            <ul className="main-menu__list">
                <li className="main-menu__item">
                    <Link className="main-menu__link" href="/subscription">
                        {t("pricing")}
                    </Link>
                </li>
                {status === "loading" ? null : session ? (
                    <>
                        <li className="main-menu__item">
                            <button
                                className="btn main-menu__link"
                                onClick={onOpenProfile}
                            >
                                {t("welcome", { name: session.user?.name || t("mysticOne") })}
                            </button>
                        </li>
                    </>
                ) : (
                    <li className="main-menu__item">
                        <button
                            className="btn main-menu__link"
                            onClick={onOpenLogin}
                        >
                            {t("revealYourself")}
                        </button>
                    </li>
                )}
            </ul>
        </nav>
    );
}
```

- [ ] **Step 4: Update `src/components/OfferBlock.tsx`**

Add `useTranslations("ui")` and replace all hardcoded strings:
- `"Discover"` → `t("discover")`
- `"Your"` → `t("your")`
- `"Fate"` → `t("fate")`
- `"Shake the Deck"` → `t("shakeDeck")`
- `"Join the Circle of the Chosen"` → `t("joinTheCircle")`
- `"Loading..."` → `t("loading")`

- [ ] **Step 5: Update `src/components/Tarot.tsx`**

Add `useTranslations("ui")` and replace:
- `"Unveil Your Destiny, Card by Card..."` → `t("unveilDestiny")`
- `"Revoke and Retry"` → `t("revokeAndRetry")`
- `"The Cards Have Spoken"` → `t("cardsHaveSpoken")`
- `"The cards are still silent..."` → `t("cardsStillSilent")`

- [ ] **Step 6: Update `src/components/LoginForm.tsx`**

Add `useTranslations("ui")` and switch `Link` to `@/i18n/navigation`. Replace all hardcoded strings:
- Form labels: `t("whatShallWeCallYou")`, `t("pledgeYourSoul")`, `t("enchantedPhrase")`
- Error messages: `t("acceptTermsError")`, `t("invalidCredentials")`, `t("somethingWentWrong")`
- Buttons: `t("channeling")`, `t("beginTheRitual")`, `t("completeTheRitual")`, `t("letGoogleSpeak")`
- Toggle text: `t("alreadyInitiated")`, `t("newToTheCraft")`
- Terms agreement: use `t.rich("agreeToTerms", { terms: (chunks) => <Link ...>{t("termsOfService")}</Link>, privacy: (chunks) => <Link ...>{t("privacyPolicy")}</Link> })`

- [ ] **Step 7: Update `src/components/Login.tsx`**

Add `useTranslations("ui")`:
- `"Step Through the Veil"` → `t("stepThroughTheVeil")`

- [ ] **Step 8: Update `src/components/UserProfile.tsx`**

Add `useTranslations("ui")` and `useTranslations("plans")`. Switch `Link` to `@/i18n/navigation`. Replace:
- Labels: `t("name")`, `t("email")`, `t("memberSince")`, `t("currentPlan")`, `t("password")`
- Buttons: `t("save")`, `t("saving")`, `t("cancel")`, `t("changePassword")`, `t("setPassword")`, `t("slipIntoShadows")`
- Placeholders: `t("currentPasswordPlaceholder")`, `t("newPasswordPlaceholder")`, `t("confirmPasswordPlaceholder")`
- Error messages: `t("nameCannotBeEmpty")`, `t("failedToUpdateName")`, `t("passwordMinLength")`, `t("passwordsDoNotMatch")`, `t("failedToUpdatePassword")`, `t("passwordUpdated")`
- Plan name: use `tPlans(\`${planId}.name\`)` instead of `PLANS[planId].name`
- `"Mystic One"` → `t("mysticOne")`
- `"→ Upgrade"` → `"→ " + t("upgrade")`

- [ ] **Step 9: Update `src/components/SubscriptionPlans.tsx`**

Add `useTranslations("ui")` and `useTranslations("plans")`. Replace:
- `"Choose your path"` → `t("chooseYourPath")`
- `"One reading at a time..."` → `t("oneReadingAtATime")`
- `"Most popular"` → `t("mostPopular")`
- `"Current plan"` → `t("currentPlanBtn")`
- `"Coming soon"` → `t("comingSoon")`
- `"Payments launching soon"` → `t("paymentsLaunchingSoon")`
- Plan name: `tPlans(\`${plan.id}.name\`)`
- Plan features: `tPlans.raw(\`${plan.id}.features\`)` to get the array, then map over it

- [ ] **Step 10: Update `src/components/PageShell.tsx`**

Add `useTranslations("ui")` and replace modal title strings:
- `"Step Through the Veil"` → `t("stepThroughTheVeil")`
- `"Your Mystic Profile"` → `t("yourMysticProfile")`

- [ ] **Step 11: Update `src/app/[locale]/page.tsx`**

Add `useTranslations("ui")` and replace modal title strings:
- `"Step Through the Veil"` → `t("stepThroughTheVeil")`
- `"Your Mystic Profile"` → `t("yourMysticProfile")`
- `"Choose Your Path"` → `t("chooseYourPath")`

- [ ] **Step 12: Verify build**

```bash
npm run build
```

Expected: Build succeeds with no TypeScript errors.

- [ ] **Step 13: Commit**

```bash
git add src/components/ src/app/
git commit -m "feat: wire up translations in all components"
```

---

## Task 9: Update legal pages to use translations

**Files:**
- Modify: `src/app/[locale]/terms/page.tsx`
- Modify: `src/app/[locale]/privacy/page.tsx`

- [ ] **Step 1: Rewrite `src/app/[locale]/terms/page.tsx`**

Replace all hardcoded legal content with translated strings from `legal.json`. Use `getTranslations` for server component metadata and `useTranslations` in the client PageShell:

```typescript
import { getTranslations, setRequestLocale } from "next-intl/server";
import { TermsContent } from "./TermsContent";

type Props = {
  params: Promise<{ locale: string }>;
};

export async function generateMetadata({ params }: Props) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "legal.terms" });
  return {
    title: t("pageTitle"),
    description: t("pageDescription"),
  };
}

export default async function TermsPage({ params }: Props) {
  const { locale } = await params;
  setRequestLocale(locale);
  return <TermsContent />;
}
```

Create `src/app/[locale]/terms/TermsContent.tsx` as a client component:

```typescript
"use client";

import { useTranslations } from "next-intl";
import { PageShell } from "@/components/PageShell";

export const TermsContent = () => {
  const t = useTranslations("legal.terms");

  const sections = [
    "entertainment", "eligibility", "accounts", "acceptableUse",
    "intellectualProperty", "userContent", "disclaimers", "liability",
    "changes", "termination", "governingLaw", "contact",
  ] as const;

  return (
    <PageShell>
      <main className="legal-page container">
        <article className="legal-page__content">
          <h1>{t("title")}</h1>
          <p><em>{t("lastUpdated")}</em></p>
          <p>{t("intro")}</p>

          {sections.map((section) => (
            <div key={section}>
              <h2>{t(`${section}.title`)}</h2>
              {section === "acceptableUse" ? (
                <>
                  <p>{t(`${section}.intro`)}</p>
                  <ul>
                    {(t.raw(`${section}.items`) as string[]).map((item, i) => (
                      <li key={i}>{item}</li>
                    ))}
                  </ul>
                </>
              ) : section === "contact" ? (
                <p>{t(`${section}.content`, { email: "support@example.com" })}</p>
              ) : (
                <p>{t(`${section}.content`)}</p>
              )}
            </div>
          ))}

          <hr />
          <p><small><strong>Note:</strong> {t("note")}</small></p>
        </article>
      </main>
    </PageShell>
  );
};
```

- [ ] **Step 2: Rewrite `src/app/[locale]/privacy/page.tsx`**

Same pattern as terms. Create `src/app/[locale]/privacy/PrivacyContent.tsx`:

Server component (`privacy/page.tsx`):
```typescript
import { getTranslations, setRequestLocale } from "next-intl/server";
import { PrivacyContent } from "./PrivacyContent";

type Props = {
  params: Promise<{ locale: string }>;
};

export async function generateMetadata({ params }: Props) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "legal.privacy" });
  return {
    title: t("pageTitle"),
    description: t("pageDescription"),
  };
}

export default async function PrivacyPage({ params }: Props) {
  const { locale } = await params;
  setRequestLocale(locale);
  return <PrivacyContent />;
}
```

Client component (`privacy/PrivacyContent.tsx`):
```typescript
"use client";

import { useTranslations } from "next-intl";
import { PageShell } from "@/components/PageShell";

export const PrivacyContent = () => {
  const t = useTranslations("legal.privacy");

  const listSections = [
    "dataWeCollect", "legalBasis", "howWeUse", "thirdParty", "yourRights",
  ] as const;

  const textSections = [
    "dataController", "transfers", "retention", "cookies",
    "security", "children", "policyChanges", "privacyContact",
  ] as const;

  const allSections = [
    "dataController", "dataWeCollect", "legalBasis", "howWeUse",
    "thirdParty", "transfers", "retention", "yourRights",
    "cookies", "security", "children", "policyChanges", "privacyContact",
  ] as const;

  return (
    <PageShell>
      <main className="legal-page container">
        <article className="legal-page__content">
          <h1>{t("title")}</h1>
          <p><em>{t("lastUpdated")}</em></p>
          <p>{t("intro")}</p>

          {allSections.map((section) => {
            const isList = (listSections as readonly string[]).includes(section);
            const hasContact = section === "dataController" || section === "privacyContact";

            return (
              <div key={section}>
                <h2>{t(`${section}.title`)}</h2>
                {isList ? (
                  <>
                    {t.has(`${section}.intro`) && <p>{t(`${section}.intro`)}</p>}
                    <ul>
                      {(t.raw(`${section}.items`) as string[]).map((item, i) => (
                        <li key={i}>{item}</li>
                      ))}
                    </ul>
                    {section === "yourRights" && <p>{t(`${section}.ccpa`)}</p>}
                  </>
                ) : hasContact ? (
                  <p>{t(`${section}.content`, { email: section === "dataController" ? "privacy@example.com" : "privacy@example.com" })}</p>
                ) : (
                  <p>{t(`${section}.content`)}</p>
                )}
              </div>
            );
          })}

          <hr />
          <p><small><strong>Note:</strong> {t("note")}</small></p>
        </article>
      </main>
    </PageShell>
  );
};
```

- [ ] **Step 3: Commit**

```bash
git add src/app/\[locale\]/terms/ src/app/\[locale\]/privacy/
git commit -m "feat: legal pages use translations from legal.json"
```

---

## Task 10: Add language switcher to header

**Files:**
- Create: `src/components/LanguageSwitcher.tsx`
- Create: `src/assets/scss/blocks/_language-switcher.scss`
- Modify: `src/assets/scss/style.scss` — import new stylesheet
- Modify: `src/components/Header.tsx` — add LanguageSwitcher

- [ ] **Step 1: Create `src/components/LanguageSwitcher.tsx`**

```typescript
"use client";

import { useLocale } from "next-intl";
import { useRouter, usePathname } from "@/i18n/navigation";
import { routing } from "@/i18n/routing";

const localeNames: Record<string, string> = {
  en: "English",
  no: "Norsk",
  ru: "Русский",
};

export const LanguageSwitcher = () => {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  const handleChange = (e: React.ChangeEvent<HTMLSelectElement>) => {
    router.replace(pathname, { locale: e.target.value });
  };

  return (
    <select
      className="language-switcher"
      value={locale}
      onChange={handleChange}
      aria-label="Language"
    >
      {routing.locales.map((loc) => (
        <option key={loc} value={loc}>
          {localeNames[loc]}
        </option>
      ))}
    </select>
  );
};
```

- [ ] **Step 2: Create `src/assets/scss/blocks/_language-switcher.scss`**

```scss
.language-switcher {
  background: transparent;
  color: $primary;
  border: $border;
  border-radius: $border-radius;
  padding: 6px 12px;
  font-family: $font-family;
  font-size: 0.85rem;
  font-weight: $general-font-weight;
  cursor: pointer;
  appearance: none;
  -webkit-appearance: none;

  option {
    background: $dark-bg;
    color: $primary;
  }
}
```

- [ ] **Step 3: Import in `src/assets/scss/style.scss`**

Add at the end:
```scss
@import "blocks/_language-switcher";
```

- [ ] **Step 4: Update `src/components/Header.tsx`**

```typescript
import Logo from "@/components/Logo";
import MainMenu from "@/components/MainMenu";
import { LanguageSwitcher } from "@/components/LanguageSwitcher";

type HeaderProps = {
    onOpenLogin: () => void;
    onOpenProfile: () => void;
};

export const Header = ({onOpenLogin, onOpenProfile}: HeaderProps) => {
    return (
        <header className="main-header container">
            <Logo />
            <MainMenu onOpenLogin={onOpenLogin} onOpenProfile={onOpenProfile} />
            <LanguageSwitcher />
        </header>
    );
};
```

- [ ] **Step 5: Verify build**

```bash
npm run build
```

Expected: Build succeeds. Language dropdown visible in header.

- [ ] **Step 6: Commit**

```bash
git add src/components/LanguageSwitcher.tsx src/components/Header.tsx src/assets/scss/
git commit -m "feat: add language switcher dropdown to header"
```

---

## Task 11: Clean up unused imports and verify end-to-end

**Files:**
- Various component files — remove unused `next/link` and `next/navigation` imports
- Remove: `src/handleAsk.tsx` and `src/utils.ts` references if `getQuestionPrompt` was only used by the old reading system

- [ ] **Step 1: Search for remaining `next/link` imports that should use `@/i18n/navigation`**

```bash
grep -r "from \"next/link\"" src/components/ src/app/
```

Replace any remaining `next/link` imports with `@/i18n/navigation` in component files. API route files and non-component files don't need locale-aware links.

- [ ] **Step 2: Search for remaining hardcoded English strings in components**

Scan all component files for any remaining hardcoded strings that were missed.

- [ ] **Step 3: Remove old imports in `src/AppProvider.tsx`**

Remove the `getQuestionPrompt` and `handleAsk` imports if they are no longer used (they were commented out).

- [ ] **Step 4: Full build and manual test**

```bash
npm run build
npm run dev
```

Test:
- Visit `/` — should redirect to `/en/`
- Visit `/en/` — English UI
- Visit `/no/` — Norwegian UI
- Visit `/ru/` — Russian UI
- Switch language via dropdown — stays on same page, locale changes
- Click "Shake the Deck" — reading appears in current language
- Click "Pricing" — goes to `/en/subscription` (or current locale)
- Legal pages render in current language
- Profile modal shows translated labels
- Login form shows translated labels

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: clean up unused imports and verify i18n end-to-end"
```
