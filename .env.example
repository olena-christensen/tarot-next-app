DATABASE_URL="postgresql://..."
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
# Public site URL used for SEO (canonical URLs, sitemap, OpenGraph).
# Set to the production domain in production env; localhost is fine for dev.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
# Zoho Mail SMTP credentials for the contact form. ZOHO_SMTP_PASS is a Zoho app
# password, not the account password — generate one in Zoho > Settings > Security.
ZOHO_SMTP_USER="founder@nothingweird.agency"
ZOHO_SMTP_PASS=""
